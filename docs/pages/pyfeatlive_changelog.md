# Py-feat Live — Changelog

<!-- Generated by `marimo-book sync-releases`. Do not edit by hand. -->

## Py-feat Live v0.8.30

_Released 2026-09-08_ · [`v0.8.30`](https://github.com/cosanlab/pyfeat-live/releases/tag/v0.8.30)

- Added signed Windows x64 installers in EXE and MSI formats, with the verified publisher **Luke Chang**.
- Enabled Windows automatic-update artifacts alongside macOS updates.
- Fixed Windows updater download links generated while a release is still a draft.

macOS Apple Silicon installers remain available.

## Py-feat Live v0.8.29

_Released 2026-09-07_ · [`v0.8.29`](https://github.com/cosanlab/pyfeat-live/releases/tag/v0.8.29)

Picks up **py-feat 2.1.3**, which pairs the AU→mesh visualization model with the v2.8 detector weights.

#### Fix

v0.8.28 shipped py-feat 2.1.2, which carries the **v2.8** multitask model but still defaulted the AU→mesh PLS to **v5** — a model fit on the **v2.5c** AU space. So `plot_face_mesh()` / `predict_face_mesh()` were mapping v2.8 AU values through a model fit on a different detector's AU distribution. 2.1.3 defaults to **v6**, fit on v2.8.

#### New AU→mesh model (v6)

Fit on 602,427 CelebV-HQ frames / 33,920 videos scored with the exact checkpoint shipped as `face_multitask_v28.safetensors`.

| | v5 | v6 |
|---|---:|---:|
| Held-out R² (5-fold, grouped by video) | 0.362 | **0.472** |

Deformations are also more anatomically localized: several AUs in v5 smeared displacement across the whole face, and its published effect-map figure rendered six AUs completely empty.

Part of the gain is a corrected extraction path — v5's training inputs were center-cropped 256→224, while v2.5+ models and py-feat's own shipped inference both resize. On v2.8 those paths differ by 0.05 mean absolute AU error and 4.6° of head pose.

#### Scope — visualization only

`/analyze` output is **bit-identical** to v0.8.28: detection, AU, emotion, gaze, pose and blendshape predictions are all unchanged. Only the AU→mesh face rendering differs.

The generate path is untouched — `pyfeat-generator` carries its own PLS geometry head and does not consume this model.

#### Note

The MPIIGaze pitch regression flagged in v0.8.28 is unchanged here; nothing in this release touches gaze.

---

## Py-feat Live v0.8.28

_Released 2026-09-06_ · [`v0.8.28`](https://github.com/cosanlab/pyfeat-live/releases/tag/v0.8.28)

Picks up **py-feat 2.1.2** (multitask **v2.8** model), and replaces a yanked pandas.

#### New model

Better AU and emotion accuracy, and better gaze on out-of-distribution and video-domain footage:

| | v2.7 | v2.8 |
|---|---:|---:|
| EYEDIAP gaze | 12.6° | **10.6°** |
| ETH-XGaze gaze | 5.1° | **4.8°** |
| Columbia gaze | 4.1° | **3.8°** |
| AffectNet-7 accuracy (end-to-end) | 0.617 | **0.632** |
| DISFA+ AU common-8 macro-F1 | 0.784 | 0.774 |

v2.8 was trained under a stricter holdout — 16 reserved splits plus identity-level exclusion of DISFA+ subjects appearing in other corpora — and is the first run whose pose distillation used correct horizontal-flip pitch mirroring.

#### Dependency fix

`pandas` moves **3.0.4 → 3.0.5**. 3.0.4 was yanked from PyPI for *"reported segfaults with datetime-related functionality"*, and the runtime lock installs verbatim into every user's venv on first launch, so it had been shipping to everyone. CI now fails on any yanked pin, and separately checks the three Tauri version files agree.

#### Known regression — worth a look during smoke-test

MPIIGaze is slightly worse than v2.7 (8.0° → 8.3°), and the weaker axis is gaze **pitch** on frontal, screen-directed gaze — exactly this app's scenario. Watch the gaze arrow's **vertical** behaviour; horizontal (yaw) tracking is unaffected and still follows the iris rather than the head.

#### Note on versioning

There is no v0.8.27. It was tagged and built before the yanked pandas was found, and discarded unpublished rather than shipped and immediately patched.

---

**Not yet verified.** Per `RELEASING.md` step 5: download the `.dmg`, install, and smoke-test before publishing — CI does not run the app. First launch installs the runtime venv (torch / py-feat / pyfeat-generator from PyPI, model weights from the `py-feat` HuggingFace org), so confirm first-run setup alongside the gaze check above.

## Py-feat Live v0.8.26

_Released 2026-08-04_ · [`v0.8.26`](https://github.com/cosanlab/pyfeat-live/releases/tag/v0.8.26)

<!-- Replace with this release's notable changes (what's new / fixes).
     Install + first-run docs: https://py-feat.org/pages/pyfeat_live/ -->

## Py-feat Live v0.8.25

_Released 2026-08-04_ · [`v0.8.25`](https://github.com/cosanlab/pyfeat-live/releases/tag/v0.8.25)

<!-- Replace with this release's notable changes (what's new / fixes).
     Install + first-run docs: https://py-feat.org/pages/pyfeat_live/ -->

## Py-feat Live v0.8.24

_Released 2026-08-04_ · [`v0.8.24`](https://github.com/cosanlab/pyfeat-live/releases/tag/v0.8.24)

<!-- Replace with this release's notable changes (what's new / fixes).
     Install + first-run docs: https://py-feat.org/pages/pyfeat_live/ -->

## Py-feat Live v0.8.22

_Released 2026-07-03_ · [`v0.8.22`](https://github.com/cosanlab/pyfeat-live/releases/tag/v0.8.22)

#### What's new in 0.8.22

##### Uses far less memory
- Playing recordings in the Viewer no longer loads the whole video file into memory
- Extracting from 4K videos no longer risks running out of memory — batches scale to the source resolution automatically

##### Faster Extract
- Running a queue of files with the same preset loads the detector once instead of per file
- Clip start times seek directly to the right spot instead of decoding everything before them; progress totals appear instantly on files that used to need a full pre-scan
- Adding a large video to the queue no longer stalls a running Live session

##### More reliable
- Rapidly labeling identities can no longer lose clicks (concurrent writes are now serialized)
- Extract progress updates can no longer get stuck showing a running job after it finished
- Identity thumbnails load much faster on long sessions
- Fixed frame numbering for variable-frame-rate videos in Extract (including re-analyzing Live recordings) and a subtle clip-start boundary error on 23.976 fps footage

Install + first-run docs: https://py-feat.org/pages/pyfeat_live/

## Py-feat Live v0.8.21

_Released 2026-07-02_ · [`v0.8.21`](https://github.com/cosanlab/pyfeat-live/releases/tag/v0.8.21)

#### What's new in 0.8.21

##### Viewer
- **Smooth playback** — the overlay now tracks the video frame-for-frame during playback (previously it updated ~4× per second)
- **Faster scrubbing** — frame lookups no longer slow down with session length, and drag-scrubbing is butter-smooth on long recordings
- **Gaze arrows point the right way** for Detectorv2 sessions (they were mirrored left/right)
- **Keyboard navigation** — ←/→ step one frame, Shift+←/→ jump 10, Home/End go to the first/last frame, space plays/pauses; scrubbing or clicking pauses playback
- Switching sessions rapidly can no longer mix one session's data into another; a loading indicator shows while a session loads
- Clicking a face to assign an identity now works correctly when the video is letterboxed

##### Workflow
- **"Open in Viewer" now works** from the Extract queue, and stopping a Live recording shows a "Recording saved — Open in Viewer" shortcut

##### Under the hood
- Live streaming uses ~90 MB less memory; assorted small fixes

Install + first-run docs: https://py-feat.org/pages/pyfeat_live/

## Py-feat Live v0.8.20

_Released 2026-07-02_ · [`v0.8.20`](https://github.com/cosanlab/pyfeat-live/releases/tag/v0.8.20)

#### What's new in 0.8.20

##### Faster, smoother Live
- Live overlay data is now prepared once per detection instead of on every poll, freeing the backend during streaming
- Stopping a recording no longer freezes the app while the video finishes writing — the UI stays responsive throughout
- Cheaper per-frame processing (faster coordinate scaling and detector downscaling)

##### Works offline
- Configuring a detector no longer contacts huggingface.co when the model weights are already downloaded — configure is near-instant and works with no network connection. First-time downloads behave as before.

##### Fixes
- Saved settings (overlay style, experimental flags) now persist reliably across launches
- Rapid stop/start of recordings can no longer corrupt a session
- The app no longer conflicts with Streamlit: it picks its own port range (18640+) and verifies it's talking to its own backend before loading
- Face thumbnails seek to the correct frame in NTSC-style videos (29.97 fps)
- Startup health checks are bounded so a busy port can't hang the launch screen

Install + first-run docs: https://py-feat.org/pages/pyfeat_live/

## Py-feat Live v0.8.19

_Released 2026-06-30_ · [`v0.8.19`](https://github.com/cosanlab/pyfeat-live/releases/tag/v0.8.19)

<!-- Replace with this release's notable changes (what's new / fixes).
     Install + first-run docs: https://py-feat.org/pages/pyfeat_live/ -->

## Py-feat Live v0.8.18

_Released 2026-06-29_ · [`v0.8.18`](https://github.com/cosanlab/pyfeat-live/releases/tag/v0.8.18)

<!-- Replace with this release's notable changes (what's new / fixes).
     Install + first-run docs: https://py-feat.org/pages/pyfeat_live/ -->

## Py-feat Live v0.8.17

_Released 2026-06-29_ · [`v0.8.17`](https://github.com/cosanlab/pyfeat-live/releases/tag/v0.8.17)

<!-- Replace with this release's notable changes (what's new / fixes).
     Install + first-run docs: https://py-feat.org/pages/pyfeat_live/ -->

## Py-feat Live v0.8.15

_Released 2026-06-18_ · [`v0.8.15`](https://github.com/cosanlab/pyfeat-live/releases/tag/v0.8.15)

#### What's new

**Facial-region overlays redesigned (Detectorv2 / mesh detectors).** The AU and
ARKit-blendshape mesh overlays now use py-feat 2.0.2's non-overlapping,
per-triangle region maps: whole, left/right-symmetric region silhouettes drawn
on the MediaPipe-478 face mesh, with the eyes and mouth left open. The live
overlay fills each region's triangles directly at its detected intensity
(monochrome heatmap), replacing the previous subdivision-smoothed rendering —
sharper region boundaries and lower per-frame cost.

- AU overlay: 19 AUs (left+right merged).
- Blendshape overlay: 33 skin-deforming ARKit blendshapes (left/right independent).

**Under the hood.** Pins py-feat==2.0.2; the bundled macOS runtime lock is
recompiled for Apple Silicon (no CUDA wheels), with the platform requirement
documented to prevent the 0.8.13-class first-run install regression.

## Py-feat Live v0.8.14

_Released 2026-06-17_ · [`v0.8.14`](https://github.com/cosanlab/pyfeat-live/releases/tag/v0.8.14)

##### Fixes
- **macOS first-run failure (regression in 0.8.13)** — 0.8.13's bundled runtime lock was compiled on a Linux/CUDA host, so it pinned CUDA-only torch deps (`cuda-bindings`/`nvidia-*`) that have no macOS wheels; the app couldn't install its Python runtime on first launch. The lock is now resolved for macOS (CPU/MPS `torch`). A CI gate dry-run-installs the lock on macOS to prevent recurrence. **0.8.13 is superseded — use 0.8.14.**

##### Facial-region overlays (Detectorv2)
- **New ARKit-blendshape mesh overlay** — toggle **Blendshapes** in Live and the Viewer (monochrome heatmap, Left/Right split at the facial midline).
- **AU mesh overlay** now uses py-feat's non-overlapping AU region map.
- Pinned **py-feat 2.0.1**.

## Py-feat Live v0.8.12

_Released 2026-06-16_ · [`v0.8.12`](https://github.com/cosanlab/pyfeat-live/releases/tag/v0.8.12)

Live and Viewer fixes, plus a Detectorv2 head-pose yaw correction.

#### Live
- **Overlay alignment** — the mesh, landmarks, box, and gaze now line up with the video. Fixed a vertical stretch that pushed the overlay up off the face ("eyes too high") on both detectors.
- **Facebox** — no longer drawn too long around Detectorv2 faces.
- **Detector switch** — switching Detectorv1 ↔ Detectorv2 keeps the right landmark style (mesh for v2, contour lines for v1) instead of getting stuck on lines.
- **Gaze direction** — corrected for both detectors.
- **Head pose (Yaw)** — Detectorv2 yaw is now derived from the stable 478-point mesh; it no longer flickers or reads the same sign when turning left vs right. Pitch and roll are unchanged.
- **Stop** — stopping the camera now reliably clears the overlay (no frozen facebox/mesh).
- **Recording frame rate** — recording no longer drops fps as severely; the recorder's frame conversion was moved off the detection path.

#### Viewer
- **Overlay/video sync** — overlays now track the video for the full clip on variable-rate live recordings (mapped by the video's real per-frame timestamps instead of a fixed fps). Fixes overlays freezing partway through while the video kept playing.
- **First frame** — fixed a mangled first-frame overlay and wrong identity thumbnail on live recordings.
- **Valence/Arousal** toggle added to the overlay toolbar.
- **Session details** — click the info icon on a session for metadata and a "Reveal in Finder" action.

#### App
- **Logs** button now works on every tab (Live, Extract, Viewer), not just Live.
- Dragging a video that misses the Extract dropzone no longer replaces the app with a bare video player.
- Logo and title moved into the header so they stay visible on every tab.

## Py-feat Live v0.8.11

_Released 2026-06-15_ · [`v0.8.11`](https://github.com/cosanlab/pyfeat-live/releases/tag/v0.8.11)

<!-- Replace with this release's notable changes (what's new / fixes).
     Install + first-run docs: https://py-feat.org/pages/pyfeat_live/ -->

## Py-feat Live v0.8.10

_Released 2026-06-14_ · [`v0.8.10`](https://github.com/cosanlab/pyfeat-live/releases/tag/v0.8.10)

#### Installation

Requires macOS on Apple Silicon (M-series).

- macOS Apple Silicon: `Py-feat.Live_*_aarch64.dmg`

First launch downloads ~1.5GB of Python dependencies and
~1GB of model weights into your user data directory.
Plan for ~10 minutes on a typical broadband connection.
Subsequent launches are instant.

## Py-feat Live v0.8.9

_Released 2026-06-14_ · [`v0.8.9`](https://github.com/cosanlab/pyfeat-live/releases/tag/v0.8.9)

#### Installation

Requires macOS on Apple Silicon (M-series).

- macOS Apple Silicon: `Py-feat.Live_*_aarch64.dmg`

First launch downloads ~1.5GB of Python dependencies and
~1GB of model weights into your user data directory.
Plan for ~10 minutes on a typical broadband connection.
Subsequent launches are instant.

## v0.8.8

_Released 2026-06-14_ · [Release notes](https://github.com/cosanlab/pyfeat-live/releases/tag/v0.8.8)

Stability + robustness release ahead of going public.

##### Detectors
- Tracks py-feat `v0.7-dev` (`3855e07`): the modular detector is now **Detectorv1** (renamed from `Detector`); old sessions/presets that recorded `"Detector"` still work via a compatibility alias.

##### Fixes (from the pre-public review)
- **Analyze**: a detection/decode error no longer leaks the recorder (writer thread + CSV handle) or orphans a session dir; a corrupt `video.mp4` is no longer left behind; one bad frame no longer fails a whole job.
- **Analyze WebSocket**: guarded JSON parsing + auto-reconnect (the queue no longer silently freezes on a dropped connection).
- **Serving**: session video/CSV stream instead of buffering the whole file into memory; malformed Analyze input returns 422 (not 500); uploaded files are cleaned up on "clear done".
- **Viewer / Live**: resilient session load + update-check (no unhandled rejections); overlay guards; the timeseries last-frame off-by-one is fixed.

##### Cleanup
- Removed dead Streamlit-era files/config/env and stale references.

This is also the first release to verify the corrected `Info.plist` (no longer requests local-network access).

## v0.8.7

_Released 2026-06-14_ · [Release notes](https://github.com/cosanlab/pyfeat-live/releases/tag/v0.8.7)

**Fixes a launch crash in v0.8.5 / v0.8.6.** Those packaged builds aborted on startup (`a webview with label 'main' already exists`) — a duplicate window definition. This release restores normal launch.

Same app features as v0.8.5/v0.8.6 (Detectorv2 v2.5, adaptive Live overlay, Viewer panels, grouped timeseries, Detectorv2-default extraction).

##### Fixed
- A label-less window in `tauri.conf.json` defaulted to the `main` label and collided with the window the app builds in code, crashing launch. Removed the duplicate.

## v0.8.6

_Released 2026-06-14_ · [Release notes](https://github.com/cosanlab/pyfeat-live/releases/tag/v0.8.6)

Hotfix release. Same app features as v0.8.5 (Detectorv2 v2.5, adaptive Live overlay, Viewer panels, grouped timeseries, Detectorv2-default extraction) — this corrects the release pipeline so the auto-updater manifest (`latest.json`) publishes.

##### Fixed
- The macOS updater-signature step searched a tripled bundle path (`target/<triple>/release/bundle/macos`) while the native build outputs to `target/release/bundle/macos`, which failed the deploy and skipped the `latest.json` publish in v0.8.5. Corrected.

## v0.8.5

_Released 2026-06-14_ · [Release notes](https://github.com/cosanlab/pyfeat-live/releases/tag/v0.8.5)

Upgrades to **Detectorv2 v2.5** and brings the Viewer in line with Live.

##### Highlights
- **Detectorv2 v2.5** — 52 MediaPipe/ARKit blendshape coefficients, safetensors weights, corrected 478-mesh decode, and head-pose pitch now reads **+up** (matching the classic Detector / Detectorv1).
- **Live** — the overlay pipeline is resolution/aspect-adaptive; it tracks correctly on any camera (no more mesh/overlay distortion on non-16:9 webcams).
- **Viewer** — emotion, valence/arousal, and pose panels now match Live and sit beside each face; the timeseries variable picker is grouped into collapsible sets (Emotions, V/A, Pose, Gaze, AUs, Blendshapes).
- **Analyze** — extraction defaults to **Detectorv2 · standard**; the classic detector is relabeled **Detectorv1**.

##### Fixes
- Mesh overlay no longer collapses/distorts under the v2.5 model.
- Head-pose pitch sign corrected to the canonical +up convention.
- `index.html` served with `no-store` to prevent stale app bundles.

## Py-feat Live v0.8.4

_Released 2026-06-04_ · [`v0.8.4`](https://github.com/cosanlab/pyfeat-live/releases/tag/v0.8.4)

#### Installation

Requires macOS on Apple Silicon (M-series).

- macOS Apple Silicon: `Py-feat.Live_*_aarch64.dmg`

First launch downloads ~1.5GB of Python dependencies and
~1GB of model weights into your user data directory.
Plan for ~10 minutes on a typical broadband connection.
Subsequent launches are instant.

## Py-feat Live v0.8.3

_Released 2026-06-03_ · [`v0.8.3`](https://github.com/cosanlab/pyfeat-live/releases/tag/v0.8.3)

#### Installation

Requires macOS on Apple Silicon (M-series).

- macOS Apple Silicon: `Py-feat.Live_*_aarch64.dmg`

First launch downloads ~1.5GB of Python dependencies and
~1GB of model weights into your user data directory.
Plan for ~10 minutes on a typical broadband connection.
Subsequent launches are instant.

## Py-feat Live v0.8.2

_Released 2026-06-03_ · [`v0.8.2`](https://github.com/cosanlab/pyfeat-live/releases/tag/v0.8.2)

#### Installation

Requires macOS on Apple Silicon (M-series).

- macOS Apple Silicon: `Py-feat.Live_*_aarch64.dmg`

First launch downloads ~1.5GB of Python dependencies and
~1GB of model weights into your user data directory.
Plan for ~10 minutes on a typical broadband connection.
Subsequent launches are instant.
